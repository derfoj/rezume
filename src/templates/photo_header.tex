% -------------------------
% Resume in Latex - Photo Header Design
% Adapted for reZume with Custom Jinja2 Delimiters
% -------------------------

\documentclass[letterpaper,11pt]{article}

\usepackage{latexsym}
\usepackage[empty]{fullpage}
\usepackage{titlesec}
\usepackage{marvosym}
\usepackage[usenames,dvipsnames]{color}
\usepackage{verbatim}
\usepackage{enumitem}
\usepackage[hidelinks]{hyperref}
\usepackage{fancyhdr}
\usepackage[english]{babel}
\usepackage{tabularx}
\usepackage{xcolor}
\usepackage{fontawesome5}
\usepackage{graphicx}
\usepackage{wrapfig}
\usepackage[margin=0.5in]{geometry}

\input{glyphtounicode}

\urlstyle{same}
\raggedbottom
\raggedright
\setlength{\tabcolsep}{0in}

% Section Formatting - Compact but readable
\titleformat{\section}{
  \vspace{-2pt}\scshape\raggedright\large\bfseries
}{}{0em}{}[\color{black}\titlerule \vspace{0pt}]

\pdfgentounicode=1

% Custom Commands
\newcommand{\resumeItem}[1]{
  \item\small{
    {#1}
  }
}

\newcommand{\resumeSubheading}[4]{
  \vspace{-1pt}\item
    \begin{tabular*}{0.97\textwidth}[t]{l@{\extracolsep{\fill}}r}
      \textbf{#1} & #2 \\
      \textit{\small#3} & \textit{\small #4} \\
    \end{tabular*}\vspace{-4pt}
}

\newcommand{\resumeProjectHeading}[2]{
    \item
    \begin{tabular*}{0.97\textwidth}{l@{\extracolsep{\fill}}r}
      \small#1 & #2 \\
    \end{tabular*}\vspace{-4pt}
}

\newcommand{\resumeSubHeadingListStart}{\begin{itemize}[leftmargin=0.15in, label={}, itemsep=1pt, parsep=0pt]}
\newcommand{\resumeSubHeadingListEnd}{\end{itemize}}
\newcommand{\resumeItemListStart}{\begin{itemize}[itemsep=1pt, parsep=0pt]}
\newcommand{\resumeItemListEnd}{\end{itemize}\vspace{-2pt}}

% -------------------- DOCUMENT START --------------------
\begin{document}

% --- HEADER WITH PHOTO ---
\noindent
\begin{minipage}[c]{0.20\textwidth}
    % Placeholder for photo validation
    (% if user.photo_path %)
    \includegraphics[width=0.9\textwidth, height=3.5cm, keepaspectratio]{(( user.photo_path ))}
    (% else %)
    \begin{center} \large \faUserCircle \end{center}
    (% endif %)
\end{minipage}%
\begin{minipage}[c]{0.78\textwidth}
    \begin{center}
        \textbf{\Huge \scshape (( user.name )) } \\ \vspace{8pt}
        \textbf{\large (( user.title )) } \\
        \small DISPONIBLE IMMÉDIATEMENT \\ \vspace{8pt}
        
        \small
        (% if user.portfolio_url %)
        \faIcon{github} \href{(( user.portfolio_url ))}{\underline{Mon Portfolio}} \quad
        (% endif %)
        
        (% if user.linkedin_url %)
        \faIcon{linkedin} \href{(( user.linkedin_url ))}{\underline{LinkedIn}} \quad
        (% endif %)
        
        \faIcon{envelope} \href{mailto:(( user.email ))}{\underline{(( user.email ))}}
    \end{center}
\end{minipage}

\vspace{10pt}

% --- EDUCATION ---
\section{EDUCATION}
\resumeSubHeadingListStart
    (% for edu in user.education %)
    \resumeSubheading
      {(( edu.institution ))}{(( edu.period ))}
      {(( edu.degree ))}{(( edu.mention )) \ifx (( edu.mention )) \empty \else \fi}
    (% endfor %)
\resumeSubHeadingListEnd

% --- EXPERIENCE ---
\section{EXPERIENCES PROFESSIONNELLES \& PROJETS}
\resumeSubHeadingListStart

(% for exp in experiences %)
   \resumeProjectHeading
    {\textbf{(( exp.title ))} $|$ \footnotesize\emph{(( exp.company ))}}{(( exp.period ))}
    \resumeItemListStart
        \resumeItem{(( exp.description ))}
    \resumeItemListEnd
    \vspace{2pt}
(% endfor %)

\resumeSubHeadingListEnd

% --- COMPÉTENCES TECHNIQUES ---
\section{COMPETENCES TECHNIQUES}
\begin{itemize}[leftmargin=0.15in, label={}]
    \small{\item{
     \textbf{Outils \& Langages}{: (( user.skills | join(', ') )) }
    }}
\end{itemize}

% --- COMPÉTENCES PERSONNELLES ---
\section{COMPETENCES PERSONNELLES}
\begin{center}
    \begin{tabularx}{\textwidth}{X X}
    (% for skill in user.soft_skills %)
       \textbf{(( skill ))}
       (% if loop.index is divisibleby 2 %) \\
       (% elif not loop.last %)&
       (% endif %)
    (% endfor %)
    \end{tabularx}
\end{center}

% --- LANGUES ---
\section{LANGUES}
\begin{itemize}[leftmargin=0.15in, label={}]
    \small{\item{
    (% for lang in user.languages %)
        \textbf{(( lang.name )):} (( lang.level )) \quad
    (% endfor %)
    }}
\end{itemize}

\end{document}
